# 30 · דוח מנהלים — תוכנית עבודה ל‑Claude Code

> העמוד שקוראים ראשון. תוכנית לבניית **אתר מבוסס‑שרת, ללא מערכת משתמשים**, מאפס.

## ציון החבילה (self‑review)
**8.5 / 10.** לולאות תיקון שרצו: 1.
- ביצועיות ה‑spec (Claude Code יכול להריץ בלי להיתקע): **9**
- חוזק הקונספט (בהקשר פרויקט אישי לא‑מסחרי): **8**
- עקביות (stories↔milestones, stack↔profile): **9**

**3 החולשות הנותרות:**
1. **בקרת קצב ב‑serverless** דורשת store משותף (Upstash חינם) — סומן ב‑`05 §6`/`26`/`27`, לא חוסם.
2. **Retention חלש יותר** בלי חשבונות — `.ics` הוא חד‑פעמי; מגושר ע"י S4 (מייל ללא חשבון) אם תרצה.
3. **ריקולים/מספר בעלים** לא מחווטים (מזהי מאגר לא אומתו) — מוצג "לא זמין" בשקיפות.

## ההחלטה
**להתקדם לבנייה — LOCAL‑FIRST.** הכיוון התייצב: אתר מבוסס‑שרת (SSR + proxy + cache) בלי חשבונות/DB —
פשוט, חינמי, נגיש. **בונים ומריצים הכל מקומית עכשיו** (`npm run dev`, אפס ענן), עם תפרים נקיים
(`CacheStore`/`RateLimiter` interfaces + env) כך שהמעבר ל‑production בהמשך יהיה **מיגרציה קצרה (2 אדפטרים
+ env), לא כתיבה מחדש** — מפורט ב‑`05 §6`. הפרוטוטייפ ב‑`src/` שירת כלמידה; התוכנית נבנתה מחדש.

## למה שרת בלי משתמשים — הגיוני
השרת מצדיק את עצמו ב‑4 דברים גם בלי חשבונות: פותר CORS, SSR ל‑SEO, cache ובקרת קצב, ובסיס להרחבה. הסרת
מערכת המשתמשים מוחקת שכבת סיכון שלמה (auth/session/IDOR/PII) ומורידה עלות ותחזוקה — רווח נקי לפרויקט אישי.

## הסיכונים המובילים
1. תלות ב‑data.gov.il (R1) — adapter+cache+ניטור. 2. נגישות נשברת (R5) — שער CI. 3. בקרת קצב serverless
(R3) — Upstash חינם. **ההנחה הריסקית:** אופי התזכורת (ics מול מייל) — קובע אם צריך DB בכלל (`26` A4).

## מה בחבילה
`00` פרופיל · `02` מחקר · `03` קונספט · `04` אפיון · `05` **build spec** · `06` עיצוב · `07` אבטחה ·
`12` נגישות · `22` DevOps · `26` הנחות · `27` סיכונים · `29` handoff kit · `08` המשך. (+ פרוטוטייפ ב‑`src/` כ‑reference.)

## הצעד הבא הטוב ביותר
למסור את `29_HANDOFF_KIT.md` → `05_BUILD_SPEC.md` ל‑Claude Code ולהתחיל מ‑M0/M1 (שלד + adapter+proxy). לפני
כן — להכריע את שתי ההנחות הפתוחות: (1) `.ics` מספיק או שרוצים S4 מייל‑ללא‑חשבון? (2) שכבת‑חינם בלבד או שדומיין בסדר?
